# grill-me-smartly review

## Source
- Task: Review the Wellness Center initialization implementation plan before execution.
- Source file/context: docs/impl-plans/2026-05-28-wellness-center-initialization.md
- Created at: 2026-05-28T13:40:45+02:00

## Plan Snapshot
The plan initializes the solo Wellness Center project by copying the group project architecture, renaming services, retheming Docker Compose, replacing seeds/assets, updating gateway and services, building EJS pages, and validating the full stack with service tests plus a PowerShell smoke test.

## Proposed Questions
| ID | Question | Main-session recommended answer |
| --- | --- | --- |
| Q1 | Is Task 1 safe and accurate for the current solo worktree state? | No. Revise Task 1 before execution to avoid overwriting or reverting durable docs. |
| Q2 | Does the rename-and-retarget strategy match the actual group project structure and service files closely enough to execute? | Mostly yes, but only if treated as repo-wide retargeting, not a simple directory rename. |
| Q3 | Is the planned browser/API route contract internally consistent with the approved solo docs? | Yes. Keep the contract, but verify implementation scans old group routes after retargeting. |
| Q4 | Do the plan and approved docs leave unresolved choices that the plan silently resolves? | Yes. Make those decisions explicit before execution. |
| Q5 | Could the final BMW-string verification fail because copied group docs/history files are staged? | Yes. Exclude or replace copied group docs before the skeleton commit. |
| Q6 | Are the npm dependency versions named in the plan likely installable and aligned with the group project manifests? | Yes. No plan-blocking change needed. |

## Subagent Answer
| ID | Answer | Evidence | Uncertainty |
| --- | --- | --- | --- |
| Q1 | Task 1 is not safe as written. | Actual status has modified tracked docs and README, not the expected untracked service folders. `Copy-Item -Force` can overwrite root files, `docs` copy can create nested `docs/docs`, and `git checkout -- docs/...` would discard the reviewed plan/spec edits. | The copy was not executed in the repo; PowerShell copy behavior was checked by inspection/temp behavior. |
| Q2 | The old service directories named in Task 1 exist, but the rename has many dependent references. | Group project contains `web-shop-frontend`, `web-shop-backend`, `car-configurator`, `merch-shop`, `route-service`, `ai-feature`, and `shopping-cart`. Docker Compose, Dockerfiles, package names/lockfiles, gateway routes, env vars, tests, views, asset prefixes, SQL files, and scripts reference old names. | Generated dependency files can be regenerated or edited during retargeting. |
| Q3 | The new browser/API route contract is internally consistent across the reviewed solo docs. | Approved pages match the plan: `/`, `/package-configurator`, `/ai-feature`, `/aftercare-shop`, `/shopping-cart`, `/visit-context`, `/impressum`. Gateway prefixes align with service-internal routes: `/api/configurator`, `/api/aftercare`, `/api/visit-context`, `/api/cart`, and `/api/ai`. | Only the requested approved docs and plan were checked, not unrelated archived docs. |
| Q4 | The plan silently resolves open decisions without contradiction. | `tech-stack.md` leaves AI fallback vs `503` open; the plan chooses `503`. Project context leaves Weather key sharing open; the plan chooses separate `GOOGLE_WEATHER_API_KEY` with MySQL fallback. Final stack state remains user-dependent in Task 13. | Source code was not inspected for this question. |
| Q5 | The final BMW-string verification is likely to fail if Task 1 copies and stages group docs/root guidance files. | Task 1 copies almost all group root entries and stages with `git add -- .`. Group `CLAUDE.md`, `README.md`, `docs/PRD.md`, `docs/project-status.md`, and `docs/team-collaboration-breakdown.md` contain BMW/old service strings. Final grep excludes only the active plan/design docs. | The copy was not executed in the solo repo. |
| Q6 | The named dependency versions are installable and mostly match the group project manifests. | Registry checks returned exact versions for `express@5.2.1`, `ejs@5.0.2`, `express-ejs-layouts@2.5.1`, `@google/genai@2.4.0`, `mysql2@3.22.3`, `redis@5.12.1`, and `http-proxy-middleware@4.0.0`. Group manifests mostly use the same versions; `route-service` would be intentionally upgraded from Express 4 to 5. | Installability was checked via registry metadata, not full `npm install` in each service. |

## Decision Packet
- Resolved locally: Q1, Q2, Q3, Q4, Q5, Q6.
- Needs real user: None for Q1.
- Needs real user: Accept or revise the plan's implicit decisions: AI returns `503` when Gemini is not configured; Weather uses separate `GOOGLE_WEATHER_API_KEY` with seeded fallback; final stack state after verification is user-selected.
- Recommended decision: Rewrite Task 1 so it treats the group project as a reference source, copies only selected runtime directories/root config files, excludes `docs`, `exchange`, `CLAUDE.md`, historical README content, and other group guidance/history files, and never uses `git checkout --` on the active plan/spec unless the user explicitly asks to discard local edits. Also make the service rename step explicitly require a repo-wide retarget of compose, Dockerfiles, package metadata, lockfiles, tests, views, SQL, assets, scripts, and route/env contracts. Add an explicit decision section for the AI, Weather, and final stack-state choices.

## Follow-up Update
- 2026-05-28: User chose the stricter approach: reference the group project first, copy code/config only as needed, and do not bring group historical documentation into the solo repo. Task 1 was updated accordingly in the implementation plan.
